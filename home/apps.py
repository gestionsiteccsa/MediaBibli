"""Configuration for the home application."""

from django.apps import AppConfig


class HomeConfig(AppConfig):
    """Django AppConfig for the home application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "home"
